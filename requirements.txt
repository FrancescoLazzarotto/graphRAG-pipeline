# Base dependencies (CPU-friendly, loose bounds for development).
#
# requirements-cpu.txt (CPU nodes) and requirements-gpu.txt (GPU nodes, CUDA
# 12.4) select the right torch build; see README "Installation". None of the
# three is pinned — they carry the same compatible-range bounds as this file,
# so two installs a month apart are not the same environment. This comment used
# to call them "pinned installs", which is what an operator would have relied on
# when a campaign stopped reproducing.
#
# For an environment that reproduces, use `requirements.lock`: exact versions,
# transitive dependencies included, captured from the environment every measured
# result was produced in. This file says what the project needs; the lock says
# what it ran with.
torch>=2.3,<3.0
langchain-community>=0.3,<0.4
langchain-neo4j
langchain-huggingface>=0.1,<0.2
langchain-core>=0.3,<0.4
# Needed by the vLLM / OpenAI-compatible generation path (llm/manager.py).
langchain-openai>=0.2,<0.4
langgraph>=0.2,<0.4
transformers>=4.45,<5.0
accelerate>=1.0,<2.0
neo4j>=5.20,<6.0
python-dotenv>=1.0,<2.0
pymupdf>=1.24,<2.0
psutil>=5.9,<9.0
faiss-cpu>=1.7,<2.0
sentence-transformers>=3.0,<4.0
# Imported at module import time by kg_pipeline (ingestion / ner / extraction /
# main) and graphrag.embeddings. Absent from every requirements file until now,
# which made CI red on a clean runner while passing locally. See
# docs/code_audit_2026-08-15.md §5.2.
pymupdf4llm>=0.0.17
gliner>=0.2,<1.0
openai>=1.40
pyyaml>=6.0,<7.0
requests>=2.31,<3.0
# Evaluation: without sacrebleu the evalkit BLEU falls back to a simplified
# local implementation — keep it installed so published metrics use the
# reference implementation.
sacrebleu>=2.4,<3.0

# Optional GPU quantization dependency:
# bitsandbytes
